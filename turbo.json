{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [""]
    },
    "serve": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    }
  }
}
